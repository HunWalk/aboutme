import styled from 'styled-components';

const Button = styled.a.attrs({
    className: 'Button'
})({
    borderColor: '#000000',
    borderWidth: 1,
    borderStyle: 'solid',
    padding: '20px',
});

export default Button;

