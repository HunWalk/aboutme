import styled from 'styled-components'
import ReactSVG from 'react-svg'

const SidebarSvgContainer = styled.div({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: '100%',
    maxHeight: '100vh',
    zIndex: '-1'
})

export default SidebarSvgContainer