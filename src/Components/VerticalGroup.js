import styled from 'styled-components'

const VerticalGroup = styled.div.attrs({
    className: 'VerticalGroup'
})({
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'flex-start'
})

export default VerticalGroup