import styled from 'styled-components'

const HorizontalGroup = styled.div.attrs({
    className: 'HorizontalGroup'
})({
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'flex-start'
})

export default HorizontalGroup